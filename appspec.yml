version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
permissions:
  - object: /var/www/html
    owner: ubuntu
    group: ubuntu
    pattern: "*"
    mode: 775
    type:
      - file
      - directory
